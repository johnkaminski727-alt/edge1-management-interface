name: Completed one-time Namecheap IMAP read canary (disarmed)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'The authorized one-time Namecheap IMAP canary completed successfully on 2026-08-21. This branch workflow is intentionally disarmed and references no repository secrets.'
