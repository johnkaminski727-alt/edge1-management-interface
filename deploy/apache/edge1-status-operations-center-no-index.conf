# Prevent Apache autoindex exposure for the public SNMP handoff directory.
# The full SNMP console remains behind the authenticated /edge1-ops/snmp/ route.
<Directory "/var/www/edge1-status/operations-center">
    Options -Indexes
</Directory>
